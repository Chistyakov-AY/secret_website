<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<p>
  <%= form.label :title %><br>
  <%= form.text_area :title, rows: 2, id: :post %>
</p>

<p>
  <%= form.label :body %><br>
  <%= form.text_area :body, rows: 2, id: :post %>
</p>

<%= form.file_field :image, remote: true %>

  <div>
    <br><%= form.submit "Create" %>
  </div>
<% end %>
